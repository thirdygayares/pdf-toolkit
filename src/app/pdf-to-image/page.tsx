import PdfToImageEntry from "./PdfToImageEntry";

/**
 * Page component that renders the PdfToImageEntry UI.
 *
 * @returns The JSX element that renders the PdfToImageEntry component
 */
export default function PdfToImagePage() {
    return <PdfToImageEntry />;
}