"use client"
import { UnsupportedConfigForm } from "./shared"
export default function PdfToImageConfig() {
    return <UnsupportedConfigForm title="PDF to Image" detail="Canvas-based page rendering is not wired into the flow engine yet. Use the standalone tool for now." />
}
