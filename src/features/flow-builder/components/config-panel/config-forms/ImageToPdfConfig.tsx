"use client"
import { UnsupportedConfigForm } from "./shared"
export default function ImageToPdfConfig() {
    return <UnsupportedConfigForm title="Image to PDF" detail="The image batch pipeline is scaffolded but not yet connected to the flow runtime." />
}
