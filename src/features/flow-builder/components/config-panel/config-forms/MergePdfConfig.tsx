"use client"
import { UnsupportedConfigForm } from "./shared"
export default function MergePdfConfig() {
    return <UnsupportedConfigForm title="Merge PDF" detail="Batch-merge semantics require grouping multiple upstream packets. The node is scaffolded, but executor support is pending." />
}
