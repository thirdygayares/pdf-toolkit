import { unsupportedExecutor } from "./_shared"
export const markdownToPdfExecutor = unsupportedExecutor("markdown-to-pdf", "Markdown-to-PDF export requires a pure byte generator adapter before flow support.")
