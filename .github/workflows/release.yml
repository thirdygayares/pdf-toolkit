name: Release
on:
  push:
    branches: ["main"]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: thirdygayares/thirdygithub/.github/workflows/semantic-release.yml@main
